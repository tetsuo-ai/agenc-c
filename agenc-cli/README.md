# `agenc-cli`

> Planned: a small declarative flag parser and a strict fail-closed JSON-subset reader built on the binary/text cursor, allocation-controlled and assuming hostile input.

Layer 9 of the family build order. Not implemented yet. The scope and
ordering live in [../LIBRARIES.md](../LIBRARIES.md); the conventions are
set by [`agenc-str`](../agenc-str/) and [`agenc-arena`](../agenc-arena/).
