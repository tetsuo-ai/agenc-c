# `agenc-err`

> Planned: a small error type (numeric code plus optional context), a stable code space fit for an ABI boundary, and propagate/cleanup macros so goto-err blocks are uniform.

Layer 3 of the family build order. Not implemented yet. The scope and
ordering live in [../LIBRARIES.md](../LIBRARIES.md); the conventions are
set by [`agenc-str`](../agenc-str/) and [`agenc-arena`](../agenc-arena/).
