# `agenc-os`

> Planned: the platform shim: read-entire-file, an mmap wrapper, path join and normalize, a monotonic clock, directory iteration, and thread/mutex wrappers over pthreads and win32.

Layer 8 of the family build order. Not implemented yet. The scope and
ordering live in [../LIBRARIES.md](../LIBRARIES.md); the conventions are
set by [`agenc-str`](../agenc-str/) and [`agenc-arena`](../agenc-arena/).
